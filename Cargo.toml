[package]
name = "MAP"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.26" # 请根据实际版本调整
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"    # 🔥 用于解析策略文件
image = { version = "0.24", default-features = false, features = ["png", "jpeg"] }
rfd = "0.12"
